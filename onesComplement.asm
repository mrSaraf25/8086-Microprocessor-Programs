;To find 1’s complement of a number.

ASSUME CS:CODE,DS:DATA
INCLUDE IO.H

DATA SEGMENT
	MSG DB 13,10,"		BEFORE : ",0
	MSG2 DB 13,10,"		AFTER  : ",0
	NUM DB 0FH
	ANS DB 6 DUP(0)
DATA ENDS

CODE SEGMENT
START:
	MOV AX,DATA
	MOV DS,AX
	
	MOV AX,0000H
	
	LEA DX,MSG
	MOV AH,02H
	INT 21H
	
	MOV AL,NUM
	OUTPUT MSG	
	ITOA ANS,AX
	OUTPUT ANS
	NOT AL
	MOV NUM,AL

	OUTPUT MSG2
	ITOA ANS,AX
	OUTPUT ANS
	
	MOV AH,4CH
	INT 21H
	CODE ENDS
END START